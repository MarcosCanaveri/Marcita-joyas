export function Navbar() {
    return (
        <navbar>Esto es la navbar</navbar>
    )
}

import { Cartwidget } from "./CartWidget"

function CartWidget(); {
  return <div>
    <CartWidget/>
  </div>
}

